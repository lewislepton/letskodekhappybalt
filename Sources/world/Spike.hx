package world;

import kha.Canvas;
import raccoon.anim.Sprite;

class Spike extends Sprite {
  public function new(x:Float, y:Float){
    super('spike', x, y, 9, 91);
  }

  override public function update(){
    super.update();
  }

  override public function render(canvas:Canvas){
    super.render(canvas);
  }
}